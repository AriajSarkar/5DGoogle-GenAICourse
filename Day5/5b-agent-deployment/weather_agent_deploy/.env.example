# Cloud configuration for deployment
# Copy this file to .env and configure for your GCP project

# Global location for Gemini models
GOOGLE_CLOUD_LOCATION="global"

# Use Vertex AI (set to 1 for production deployment)
GOOGLE_GENAI_USE_VERTEXAI=1

# Optional: Specify your GCP project (can also be set via gcloud config)
# GOOGLE_CLOUD_PROJECT=your-project-id

# Optional: Model overrides (uses defaults from utils/model_config.py if not set)
# GEMINI_TEXT_MODEL=gemini-2.5-flash-lite
# GEMINI_MULTIMODAL_MODEL=gemini-2.0-flash-preview-image-generation
# GEMINI_PRO_MODEL=gemini-2.5-pro
